########################################
# FOR THE TESTS WITH A LOCAL DATA API: #
########################################

# Authentication:
export LOCAL_DATA_API_USERNAME="cassandra"
export LOCAL_DATA_API_PASSWORD="cassandra"

export LOCAL_DATA_API_ENDPOINT="http://localhost:8181"
export LOCAL_CASSANDRA_CONTACT_POINT="127.0.0.1"
# OPTIONAL (default if omitted):
# export LOCAL_CASSANDRA_PORT="9042"

# OPTIONAL. Default keyspace names if omitted. Keyspaces are created if needed.
# export LOCAL_DATA_API_KEYSPACE="..."
# export LOCAL_DATA_API_SECONDARY_KEYSPACE="..."

# RERANKER SETTINGS for non-Astra runs (Optional).
# Uncomment and supply a DEV Astra token to run the related tests:
# export HEADER_RERANKING_API_KEY_NVIDIA="AstraCS:xyz...abc..."
